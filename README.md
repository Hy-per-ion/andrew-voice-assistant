Talk with ANDREW voice assistant

Install follwing packages before running:

pip install requests

pip install SpeechRecognition

pip install pyjokes

pip install pyttsx3

pip install flask

pip install pywhatkit